import BlockItem from './BlockItem';

export default { 
  title: 'Components/BlockItem',
  component: BlockItem,
};

const Template = () => (
  <BlockItem />
);

export const Default = Template.bind({});


