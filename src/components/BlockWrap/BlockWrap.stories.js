import BlockWrap from './BlockWrap';

export default { 
  title: 'Components/BlockWrap',
  component: BlockWrap,
};

const Template = () => (
  <BlockWrap />
);

export const Default = Template.bind({});


